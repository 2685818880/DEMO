# 出库任务下发定时器使用说明

## 概述

出库任务下发定时器是一个自动化的调度服务，用于定期检查状态为"就绪"的出库任务，并自动将其下发到WCS系统。该定时器可以显著提高出库作业的自动化程度和效率。

## 功能特性

1. **自动检查**：每30秒自动检查一次待下发的出库任务
2. **智能过滤**：自动过滤掉容器已有活跃任务的任务
3. **状态管理**：自动更新出库任务状态（从Created到Executing）
4. **任务下发**：自动创建调度任务并下发到WCS系统
5. **手动触发**：支持手动触发出库任务下发
6. **错误处理**：完善的异常处理和日志记录
7. **事务支持**：确保数据一致性

## 系统架构

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  出库任务定时器  │───▶│  任务查询服务   │───▶│  数据库查询     │
│  (Scheduler)    │    │  (Query Service)│    │  (Database)     │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  任务下发服务    │───▶│  调度管理器     │───▶│  WCS系统        │
│  (Dispatch)     │    │  (Dispatch Mgr) │    │  (WCS System)   │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

## 安装和配置

### 1. 启用定时任务功能

定时器已经通过`SchedulingConfig`类启用了Spring的定时任务功能：

```java
@Configuration
@EnableScheduling
public class SchedulingConfig {
    // 启用定时任务功能
}
```

### 2. 配置定时器参数

定时器的主要配置参数：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| fixedDelay | 30000 | 执行间隔（毫秒），每30秒执行一次 |
| initialDelay | 10000 | 初始延迟（毫秒），应用启动后10秒开始执行 |
| rollbackFor | Exception.class | 事务回滚异常类型 |

### 3. 配置文件

可以在`application-scheduler.yml`中配置定时器的详细参数：

```yaml
outbound:
  scheduler:
    enabled: true
    fixed-delay: 30000
    initial-delay: 10000
    max-concurrent-tasks: 5
```

## 使用方法

### 1. 自动执行

定时器会自动执行，无需手动干预。系统启动后，定时器会：
1. 每30秒检查一次状态为"Created"的出库任务
2. 过滤掉容器已有活跃任务的任务
3. 自动下发出库任务到WCS系统
4. 更新出库任务状态为"Executing"

### 2. 手动触发

可以通过`OutboundTaskScheduler`类的`triggerManualDispatch`方法手动触发出库任务下发：

```java
@Autowired
private OutboundTaskScheduler outboundTaskScheduler;

// 手动触发出库任务下发
boolean success = outboundTaskScheduler.triggerManualDispatch("OB202502270001");
if (success) {
    log.info("出库任务手动下发成功");
} else {
    log.error("出库任务手动下发失败");
}
```

### 3. 监控日志

定时器会输出详细的日志信息，可以通过以下方式监控：

```log
2026-02-27 15:30:00 [outbound-scheduler-1] INFO  OutboundTaskScheduler - 【出库任务定时器】开始检查待下发的出库任务...
2026-02-27 15:30:00 [outbound-scheduler-1] INFO  OutboundTaskScheduler - 【出库任务定时器】找到 2 个待下发的出库任务
2026-02-27 15:30:00 [outbound-scheduler-1] INFO  OutboundTaskScheduler - 【出库任务下发】开始下发任务: formNo=OB001, containerCode=TP001, locationCode=LOC001, outStation=STATION001
2026-02-27 15:30:01 [outbound-scheduler-1] INFO  OutboundTaskScheduler - 【出库任务状态更新】成功: formNo=OB001, status=Executing
2026-02-27 15:30:01 [outbound-scheduler-1] INFO  OutboundTaskScheduler - 【出库任务下发】任务发布成功: formNo=OB001, jobId=123456
2026-02-27 15:30:01 [outbound-scheduler-1] INFO  OutboundTaskScheduler - 【出库任务定时器】处理完成: 成功=1, 失败=0, 总计=1
```

## 业务流程

### 1. 任务查询流程
```
开始
  ↓
查询状态为"Created"的出库任务
  ↓
过滤容器编码为空的任务
  ↓
过滤库位编码为空的任务
  ↓
过滤容器已有活跃任务的任务
  ↓
按创建时间升序排序
  ↓
返回待下发任务列表
```

### 2. 任务下发流程
```
开始
  ↓
检查容器是否已有活跃任务（双重检查）
  ↓
创建运输调度参数
  ↓
创建调度信息
  ↓
执行调度生成调度任务
  ↓
更新出库任务状态为"Executing"
  ↓
发布任务到WCS系统
  ↓
记录成功日志
  ↓
结束
```

## 错误处理

### 1. 常见错误及处理

| 错误类型 | 原因 | 处理方式 |
|----------|------|----------|
| 容器已有活跃任务 | 容器正在执行其他任务 | 跳过该任务，记录警告日志 |
| 调度信息创建失败 | 参数错误或系统异常 | 记录错误日志，任务下发失败 |
| 调度执行失败 | 调度器异常或参数错误 | 记录错误日志，任务下发失败 |
| 数据库更新失败 | 数据库连接异常 | 记录错误日志，任务下发失败 |

### 2. 重试机制

定时器本身具有重试机制：
- 每次执行都会重新检查所有待下发任务
- 失败的任务会在下一次定时执行时重新尝试
- 支持手动重试通过`triggerManualDispatch`方法

## 性能优化

### 1. 查询优化
- 使用索引优化数据库查询
- 限制每次查询的任务数量
- 按创建时间排序，优先处理较早的任务

### 2. 并发控制
- 限制最大并发任务数
- 避免同一容器同时下发多个任务
- 使用事务确保数据一致性

### 3. 资源管理
- 合理配置线程池大小
- 监控内存使用情况
- 定期清理日志文件

## 测试验证

### 1. 单元测试
运行单元测试验证定时器功能：

```bash
mvn test -Dtest=OutboundTaskSchedulerTest
```

### 2. 集成测试
1. 创建测试出库任务（状态为Created）
2. 等待定时器执行（约30秒）
3. 验证任务状态是否更新为Executing
4. 验证WCS系统是否收到任务

### 3. 性能测试
1. 批量创建100个出库任务
2. 监控定时器执行时间
3. 验证所有任务是否正常下发
4. 监控系统资源使用情况

## 维护和监控

### 1. 日志监控
- 监控`OutboundTaskScheduler`类的日志输出
- 设置日志级别为INFO或DEBUG进行调试
- 定期分析日志文件，发现问题

### 2. 性能监控
- 监控定时器执行频率
- 监控任务下发成功率
- 监控系统资源使用情况

### 3. 故障处理
1. **定时器停止运行**：检查应用日志，重启应用
2. **任务下发失败**：检查WCS系统连接，验证参数配置
3. **数据库连接异常**：检查数据库连接配置，验证网络连通性

## 注意事项

1. **容器编码必须有效**：出库任务必须有有效的容器编码
2. **库位编码必须有效**：出库任务必须有有效的库位编码
3. **避免重复下发**：定时器会自动过滤已有活跃任务的容器
4. **事务一致性**：任务下发和状态更新在同一个事务中
5. **日志记录**：所有操作都会记录详细日志，便于问题排查

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0.0 | 2026-02-27 | 初始版本，实现基本定时下发功能 |
| 1.0.1 | 2026-02-27 | 修复编译错误，优化日志输出 |

## 技术支持

如有问题，请联系：
- 开发团队：Cline
- 邮箱：support@example.com
- 文档版本：1.0.0